# cesta k SQLite databáze (relatívne ku koreňu repa)
DB_PATH=data/listings.db

# port API servera
API_PORT=3001

# pauza medzi HTTP requestmi na portál v ms (nikdy nedávaj pod 1000)
SCRAPE_DELAY_MS=1500

# ak je 1, scraper číta stránky z data/cache namiesto siete (vývoj)
SCRAPE_USE_CACHE=1
